# www redirect is handled via Cloudflare Dashboard → Rules → Redirect Rules
# (CF Pages _redirects does not support full-URL source matching)

# If you add subpages later, use path-only rules like:
# /old-page /new-page 301
